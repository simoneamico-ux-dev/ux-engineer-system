---
sidebar_position: 13
sidebar_label: 'Rock, Paper, Scissors Game'
title: 'Rock, Paper, Scissors Game'
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Rock, Paper, Scissors Game

<img width="100%" alt="Rock, paper, scissors game interface" src="https://github.com/user-attachments/assets/e859c542-2c7d-4efb-a024-c08a20970325" />
<img width="100%" alt="Computer victory interface" src="https://github.com/user-attachments/assets/58f883c9-fbaa-4680-a60e-2671c93de526" />

### The Project
Interactive Rock, Paper, Scissors game developed with vanilla JavaScript, DOM manipulation and complete game logic. A review project that consolidates all concepts learned so far.

### Source Code

<Tabs>
<TabItem value="html" label="index.html" default>

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Rock, Paper, Scissors game</title>
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <h1>Let's play Rock, Paper, Scissors!</h1>
    <main>
      <details class="rules-container">
        <summary>Rules to the game</summary>

        <p>You will be playing against the computer.</p>
        <p>You can choose between Rock, Paper, and Scissors.</p>
        <p>The first one to three points wins.</p>

        <p>Here are the rules to getting a point in the game:</p>
        <ul>
          <li>Rock beats Scissors</li>
          <li>Scissors beats Paper</li>
          <li>Paper beats Rock</li>
        </ul>
        <p>
          If the player and computer choose the same option (Ex. Paper and
          Paper), then no one gets the point.
        </p>
      </details>

      <div class="score-container">
        <strong
          >Player Score: <span class="score" id="player-score">0</span></strong
        >
        <strong
          >Computer Score:
          <span class="score" id="computer-score">0</span></strong
        >
      </div>

      <section class="options-container">
        <h2>Choose an option:</h2>
        <div class="btn-container">
          <button id="rock-btn" class="btn">Rock</button>
          <button id="paper-btn" class="btn">Paper</button>
          <button id="scissors-btn" class="btn">Scissors</button>
        </div>
      </section>

      <div class="results-container">
        <p id="results-msg"></p>
        <p id="winner-msg"></p>
        <button class="btn" id="reset-game-btn">Play again?</button>
      </div>
    </main>
    <script src="./script.js"></script>
  </body>
</html>

```
</TabItem>

<TabItem value="css" label="styles.css">

```css
*,
*::before,
*::after {
margin: 0;
padding: 0;
box-sizing: border-box;
}

:root {
--very-dark-blue: #0a0a23;
--white: #ffffff;
--yellow: #f1be32;
--golden-yellow: #feac32;
}

body {
background-color: var(--very-dark-blue);
text-align: center;
color: var(--white);
}

h1 {
margin: 15px 0 20px;
}

.btn {
cursor: pointer;
width: 100px;
margin: 10px;
color: var(--very-dark-blue);
background-color: var(--golden-yellow);
background-image: linear-gradient(#fecc4c, #ffac33);
border-color: var(--golden-yellow);
border-width: 3px;
}

.btn:hover {
background-image: linear-gradient(#ffcc4c, #f89808);
}

.rules-container {
padding: 10px 0;
margin: auto;
border-radius: 15px;
border: 5px solid var(--yellow);
background-color: var(--white);
color: var(--very-dark-blue);
}

.rules-container ul {
list-style-type: none;
}

.rules-container p {
margin: 10px 0;
}

@media (min-width: 760px) {
.rules-container {
    width: 60%;
}
}

.score-container {
display: flex;
justify-content: space-around;
margin: 30px 0;
font-size: 1.2rem;
}

.score {
font-weight: 500;
}

.results-container {
font-size: 1.3rem;
margin: 15px 0;
}

#winner-msg {
margin-top: 25px;
}

#reset-game-btn {
display: none;
margin: 20px auto;
}
```
</TabItem>

<TabItem value="js" label="script.js">

```js
function getRandomComputerResult() {
const options = ["Rock", "Paper", "Scissors"];
const randomIndex = Math.floor(Math.random() * options.length);
return options[randomIndex];
}

function hasPlayerWonTheRound(player, computer) {
return (
    (player === "Rock" && computer === "Scissors") ||
    (player === "Scissors" && computer === "Paper") ||
    (player === "Paper" && computer === "Rock")
);
}

let playerScore = 0;
let computerScore = 0;

function getRoundResults(userOption) {
const computerResult = getRandomComputerResult();

if (hasPlayerWonTheRound(userOption, computerResult)) {
    playerScore++;
    return `Player wins! ${userOption} beats ${computerResult}`;
} else if (computerResult === userOption) {
    return `It's a tie! Both chose ${userOption}`;
} else {
    computerScore++;
    return `Computer wins! ${computerResult} beats ${userOption}`;
}
}

const playerScoreSpanElement = document.getElementById("player-score");
const computerScoreSpanElement = document.getElementById("computer-score");
const roundResultsMsg = document.getElementById("results-msg");
const winnerMsgElement = document.getElementById("winner-msg");
const optionsContainer = document.querySelector(".options-container");
const resetGameBtn = document.getElementById("reset-game-btn");

function showResults(userOption) {
roundResultsMsg.innerText = getRoundResults(userOption);
computerScoreSpanElement.innerText = computerScore;
playerScoreSpanElement.innerText = playerScore;

if (playerScore === 3 || computerScore === 3) {
    winnerMsgElement.innerText = `${
    playerScore === 3 ? "Player" : "Computer"
    } has won the game!`;

    resetGameBtn.style.display = "block";
    optionsContainer.style.display = "none";
}

};
function resetGame() {
playerScore = 0;
computerScore = 0;
playerScoreSpanElement.innerText = "0";
computerScoreSpanElement.innerText = "0";
resetGameBtn.style.display = "none";
optionsContainer.style.display = "block";
winnerMsgElement.innerText = "";
roundResultsMsg.innerText = "";
};

resetGameBtn.addEventListener("click", resetGame);

const rockBtn = document.getElementById("rock-btn");
const paperBtn = document.getElementById("paper-btn");
const scissorsBtn = document.getElementById("scissors-btn");

rockBtn.addEventListener("click", function () {
showResults("Rock");
});

paperBtn.addEventListener("click", function () {
showResults("Paper");
});

scissorsBtn.addEventListener("click", function () {
showResults("Scissors");
});
```

</TabItem> 

<TabItem value="commented" label="explained">

```html
<!DOCTYPE html>
<!-- 🎯 HTML5 DECLARATION: "Welcome to the Rock, Paper, Scissors arena!" -->
<!-- It's like opening the doors to a digital tournament! ✂️📄🪨 -->

<html lang="en">
<!-- 🌍 MAIN CONTAINER: The ring of our game -->
<!-- lang="en" = English, the universal language of gaming! 🎮 -->

  <head>
    <!-- 🧠 THE BRAIN OF THE GAME: Where we prepare the rules and style -->
    <!-- It's like setting up the game table before the match -->
    
    <meta charset="UTF-8" />
    <!-- 📝 UNIVERSAL CHARACTERS: UTF-8 for emojis and symbols! ✂️📄🪨 -->
    
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- 📱 RESPONSIVE: "Play on any device!" -->
    
    <title>Rock, Paper, Scissors game</title>
    <!-- 📑 TITLE: The name of our digital tournament -->
    
    <style>
      /* 🎨 ===== CSS RESET: Total Cleanup ===== */
      
      *,
      *::before,
      *::after {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        /* 🧹 TOTAL CLEANUP: Reset everything to start clean */
        /* Includes pseudo-elements too! */
      }
      
      /* 🎨 ===== COLOR PALETTE: The Game Colors ===== */
      
      :root {
        --very-dark-blue: #0a0a23;
        /* 🌑 NIGHT BLUE: The dark arena background */
        
        --white: #ffffff;
        /* ⚪ WHITE: For text and contrast */
        
        --yellow: #f1be32;
        /* 🟡 YELLOW: For rule borders */
        
        --golden-yellow: #feac32;
        /* 🏆 GOLDEN YELLOW: For buttons, victory color! */
      }
      
      /* 🎮 ===== BODY STYLE ===== */
      
      body {
        background-color: var(--very-dark-blue);
        /* 🌑 DARK BACKGROUND: Like a nighttime arena */
        
        text-align: center;
        /* ⚖️ ALL CENTERED: The game is the center of attention */
        
        color: var(--white);
        /* ⚪ WHITE TEXT: Perfect contrast */
      }
      
      h1 {
        margin: 15px 0 20px;
        /* 📏 MARGINS: 15px top, 0 sides, 20px bottom */
      }
      
      /* 🔘 ===== BUTTON STYLE ===== */
      
      .btn {
        cursor: pointer;
        /* 👆 POINTING HAND: "I'm clickable!" */
        
        width: 100px;
        /* 📏 FIXED WIDTH: All buttons the same */
        
        margin: 10px;
        /* 🌬️ SPACE: Breathing room between buttons */
        
        color: var(--very-dark-blue);
        /* 🌑 DARK TEXT: On light background */
        
        background-color: var(--golden-yellow);
        /* 🏆 GOLDEN BACKGROUND: Base for gradient */
        
        background-image: linear-gradient(#fecc4c, #ffac33);
        /* 🌅 GRADIENT: From light yellow to golden */
        /* Creates a 3D effect on buttons! */
        
        border-color: var(--golden-yellow);
        border-width: 3px;
        /* 🖼️ BORDER: 3px of the same color */
      }
      
      .btn:hover {
        background-image: linear-gradient(#ffcc4c, #f89808);
        /* 🔥 HOVER: More intense gradient, more orange */
        /* The button "warms up" when the mouse passes over! */
      }
      
      /* 📋 ===== RULES CONTAINER ===== */
      
      .rules-container {
        padding: 10px 0;
        /* 🏠 VERTICAL PADDING: Only top and bottom */
        
        margin: auto;
        /* ⚖️ CENTERING: margin auto centers horizontally */
        
        border-radius: 15px;
        /* 🔄 SOFT CORNERS: 15px rounding */
        
        border: 5px solid var(--yellow);
        /* 🖼️ YELLOW FRAME: 5px thick for importance */
        
        background-color: var(--white);
        /* ⚪ WHITE BACKGROUND: For readability */
        
        color: var(--very-dark-blue);
        /* 🌑 DARK TEXT: On light background */
      }
      
      .rules-container ul {
        list-style-type: none;
        /* 🚫 NO BULLETS: Clean lists */
      }
      
      .rules-container p {
        margin: 10px 0;
        /* 📏 PARAGRAPH SPACING: Breathing room between lines */
      }
      
      @media (min-width: 760px) {
        /* 🖥️ DESKTOP: Screens at least 760px wide */
        
        .rules-container {
          width: 60%;
          /* 📏 REDUCED WIDTH: Only 60% on desktop */
          /* On mobile it's 100% (default) */
        }
      }
      
      /* 📊 ===== SCORE CONTAINER ===== */
      
      .score-container {
        display: flex;
        /* 🎯 FLEXBOX: For aligning scores */
        
        justify-content: space-around;
        /* ⚖️ UNIFORM SPACE: Distributes scores */
        /* space-around = equal space around elements */
        
        margin: 30px 0;
        /* 📏 VERTICAL MARGINS: 30px top and bottom */
        
        font-size: 1.2rem;
        /* 📏 LARGER FONT: 120% of base size */
      }
      
      .score {
        font-weight: 500;
        /* 💪 SEMI-BOLD: Numbers stand out */
      }
      
      /* 📢 ===== RESULTS CONTAINER ===== */
      
      .results-container {
        font-size: 1.3rem;
        /* 📏 LARGE FONT: For important messages */
        
        margin: 15px 0;
        /* 📏 MARGINS: Vertical space */
      }
      
      #winner-msg {
        margin-top: 25px;
        /* ⬆️ SPACE ABOVE: Separates the final message */
      }
      
      #reset-game-btn {
        display: none;
        /* 🚫 HIDDEN: Appears only at end of game */
        
        margin: 20px auto;
        /* ⚖️ CENTERED: With vertical space */
      }
    </style>
  </head>
  
  <body>
    <!-- 🎮 ===== THE GAME COMES TO LIFE! ===== -->
    
    <h1>Let's play Rock, Paper, Scissors!</h1>
    <!-- 🎯 TITLE: The invitation to the game! -->
    
    <main>
      <!-- 📄 MAIN CONTENT: The game arena -->
      
      <details class="rules-container">
        <!-- 📋 EXPANDABLE DETAILS: Native HTML5! -->
        
        <summary>Rules to the game</summary>
        <!-- 🔽 SUMMARY: Click to open/close -->
        <!-- details/summary = native accordion without JavaScript! -->

        <p>You will be playing against the computer.</p>
        <p>You can choose between Rock, Paper, and Scissors.</p>
        <p>The first one to three points wins.</p>
        <!-- 📜 BASIC RULES: Simple and clear -->

        <p>Here are the rules to getting a point in the game:</p>
        <ul>
          <li>Rock beats Scissors</li>
          <!-- 🪨 > ✂️ Rock breaks scissors -->
          
          <li>Scissors beats Paper</li>
          <!-- ✂️ > 📄 Scissors cut paper -->
          
          <li>Paper beats Rock</li>
          <!-- 📄 > 🪨 Paper wraps rock -->
        </ul>
        <p>
          If the player and computer choose the same option (Ex. Paper and
          Paper), then no one gets the point.
        </p>
        <!-- 🤝 TIE: Same choice = no points -->
      </details>

      <div class="score-container">
        <!-- 📊 SCORE BOARD: The match counter -->
        
        <strong
          >Player Score: <span class="score" id="player-score">0</span></strong
        >
        <!-- 👤 PLAYER SCORE: Starts at 0 -->
        
        <strong
          >Computer Score:
          <span class="score" id="computer-score">0</span></strong
        >
        <!-- 🤖 COMPUTER SCORE: Also starts at 0 -->
      </div>

      <section class="options-container">
        <!-- 🎯 OPTIONS SECTION: The game controls -->
        
        <h2>Choose an option:</h2>
        <!-- 📢 INSTRUCTIONS: Clear and direct -->
        
        <div class="btn-container">
          <!-- 🔘 BUTTON CONTAINER: The three choices -->
          
          <button id="rock-btn" class="btn">Rock</button>
          <!-- 🪨 ROCK: First option -->
          
          <button id="paper-btn" class="btn">Paper</button>
          <!-- 📄 PAPER: Second option -->
          
          <button id="scissors-btn" class="btn">Scissors</button>
          <!-- ✂️ SCISSORS: Third option -->
        </div>
      </section>

      <div class="results-container">
        <!-- 📢 RESULTS AREA: Where messages appear -->
        
        <p id="results-msg"></p>
        <!-- 📝 ROUND MESSAGE: Who won this turn -->
        
        <p id="winner-msg"></p>
        <!-- 🏆 FINAL MESSAGE: Who won the game -->
        
        <button class="btn" id="reset-game-btn">Play again?</button>
        <!-- 🔄 RESET BUTTON: Hidden until the end -->
      </div>
    </main>
    
    <script>
      /* 🧠 ===== GAME LOGIC: JavaScript in Action! ===== */
      
      /* 🎲 ===== FUNCTION: Random Computer Choice ===== */
      function getRandomComputerResult() {
        const options = ["Rock", "Paper", "Scissors"];
        // 📋 OPTIONS ARRAY: The three possible choices
        
        const randomIndex = Math.floor(Math.random() * options.length);
        // 🎲 RANDOM NUMBER: The magic formula!
        // Math.random() = number between 0 and 0.999...
        // * options.length = multiply by 3 (number of options)
        // Math.floor() = round down
        // Result: 0, 1, or 2 (array indices)
        
        return options[randomIndex];
        // 🎯 RETURN: The randomly chosen option
      }
      
      /* 🏆 ===== FUNCTION: Who Wins the Round? ===== */
      function hasPlayerWonTheRound(player, computer) {
        return (
          (player === "Rock" && computer === "Scissors") ||
          (player === "Scissors" && computer === "Paper") ||
          (player === "Paper" && computer === "Rock")
        );
        // 🧮 WINNING LOGIC: All winning combinations
        // || = logical OR, only one condition needs to be true
        // && = logical AND, both must be true
        // Returns true if player wins, false otherwise
      }
      
      /* 📊 ===== GLOBAL VARIABLES: The Counters ===== */
      let playerScore = 0;
      let computerScore = 0;
      // 🔢 SCORES: Start at zero, let because they will change
      
      /* 🎮 ===== FUNCTION: Round Management ===== */
      function getRoundResults(userOption) {
        const computerResult = getRandomComputerResult();
        // 🤖 COMPUTER CHOICE: Calls the random function
      
        if (hasPlayerWonTheRound(userOption, computerResult)) {
          playerScore++;
          // 👤 PLAYER VICTORY: Increment score
          // ++ = increment by 1
          
          return `Player wins! ${userOption} beats ${computerResult}`;
          // 📢 VICTORY MESSAGE: Template literal with variables
        
        } else if (computerResult === userOption) {
          return `It's a tie! Both chose ${userOption}`;
          // 🤝 TIE: Same choice
        
        } else {
          computerScore++;
          // 🤖 COMPUTER VICTORY: Increment its score
          
          return `Computer wins! ${computerResult} beats ${userOption}`;
          // 📢 DEFEAT MESSAGE: Computer has won
        }
      }
      
      /* 🎯 ===== DOM ELEMENTS SELECTION ===== */
      const playerScoreSpanElement = document.getElementById("player-score");
      const computerScoreSpanElement = document.getElementById("computer-score");
      const roundResultsMsg = document.getElementById("results-msg");
      const winnerMsgElement = document.getElementById("winner-msg");
      const optionsContainer = document.querySelector(".options-container");
      const resetGameBtn = document.getElementById("reset-game-btn");
      // 🔍 DOM CONNECTIONS: All elements we'll modify
      // getElementById = select by ID (faster)
      // querySelector = select by CSS class (more flexible)
      
      /* 🎭 ===== FUNCTION: Show Results ===== */
      function showResults(userOption) {
        roundResultsMsg.innerText = getRoundResults(userOption);
        // 📝 SHOW RESULT: Update round message
        
        computerScoreSpanElement.innerText = computerScore;
        playerScoreSpanElement.innerText = playerScore;
        // 📊 UPDATE SCORES: Show new numbers
      
        if (playerScore === 3 || computerScore === 3) {
          // 🏁 GAME OVER: Someone has reached 3 points!
          
          winnerMsgElement.innerText = `${
            playerScore === 3 ? "Player" : "Computer"
          } has won the game!`;
          // 🏆 ANNOUNCE WINNER: Elegant ternary operator
          // condition ? if_true : if_false
      
          resetGameBtn.style.display = "block";
          // 🔄 SHOW RESET: From none to block
          
          optionsContainer.style.display = "none";
          // 🚫 HIDE OPTIONS: Can't play anymore
        }
      };
      
      /* 🔄 ===== FUNCTION: Game Reset ===== */
      function resetGame() {
        playerScore = 0;
        computerScore = 0;
        // 🔢 RESET SCORES: Start from scratch
        
        playerScoreSpanElement.innerText = "0";
        computerScoreSpanElement.innerText = "0";
        // 📊 UPDATE DISPLAY: Show zeros
        
        resetGameBtn.style.display = "none";
        // 🚫 HIDE RESET: Back to hidden
        
        optionsContainer.style.display = "block";
        // 🎯 SHOW OPTIONS: Can play again
        
        winnerMsgElement.innerText = "";
        roundResultsMsg.innerText = "";
        // 🧹 CLEAR MESSAGES: Clean slate
      };
      
      /* 🎧 ===== EVENT LISTENERS: Game Controls ===== */
      
      resetGameBtn.addEventListener("click", resetGame);
      // 🔄 CLICK RESET: When clicked, reset everything
      
      const rockBtn = document.getElementById("rock-btn");
      const paperBtn = document.getElementById("paper-btn");
      const scissorsBtn = document.getElementById("scissors-btn");
      // 🔘 BUTTON SELECTION: The three main controls
      
      rockBtn.addEventListener("click", function () {
        showResults("Rock");
      });
      // 🪨 CLICK ROCK: Play "Rock"
      
      paperBtn.addEventListener("click", function () {
        showResults("Paper");
      });
      // 📄 CLICK PAPER: Play "Paper"
      
      scissorsBtn.addEventListener("click", function () {
        showResults("Scissors");
      });
      // ✂️ CLICK SCISSORS: Play "Scissors"
      
      // MODERN ALTERNATIVE:
      // scissorsBtn.addEventListener("click", () => showResults("Scissors"));
      // More concise arrow function!
      
      /* 🎯 ===== GAME FLOW ===== */
      // 1. Player clicks a button
      // 2. showResults() is called with the choice
      // 3. Computer makes random choice
      // 4. Determines round winner
      // 5. Updates scores and messages
      // 6. Checks if someone has won (3 points)
      // 7. If yes, shows winner and reset button
      // 8. Click on reset starts everything over
    </script>
  </body>
</html>

<!-- 🎯 ===== GAME SUMMARY ===== -->
<!-- 
🎮 ROCK PAPER SCISSORS ARCHITECTURE:
│
├── 🎨 HTML/CSS (interface)
│   ├── 📋 Details/Summary for expandable rules
│   ├── 📊 Score container with flexbox
│   ├── 🔘 Golden buttons with gradients
│   └── 📱 Responsive with media query
│
└── 🧠 JavaScript (logic)
    ├── 🎲 Random selection for computer
    ├── 🏆 Winning logic with logical operators
    ├── 📊 Global score management
    ├── 🎭 Dynamic DOM manipulation
    └── 🎧 Event listeners for interactivity

💡 KEY JAVASCRIPT CONCEPTS:
├── Math.random() for randomness
├── Template literals for dynamic strings
├── Ternary operator for inline decisions
├── getElementById vs querySelector
├── Event listeners for interactivity
└── style.display manipulation for show/hide

It's like building a digital referee that manages an eternal match! 🎮✨
-->
```
</TabItem>

</Tabs>

### The Cognitive Intensity of Review

This project was quite cognitively demanding, although it was composed of only 6 steps, each one required me to conduct a total review of most of the concepts learned.

The vademecum fit like a glove and it was super interesting to modify it as I progressed.

You could say this project functioned as a "validator" of the vademecum: I based myself on it and improved it precisely based on concepts that needed greater clarity, completeness and also the addition of more concise and efficient syntax.

### The Challenge of "JavaScript Only"

Sometimes, like this time, you don't start by writing HTML and CSS, but with JavaScript alone. I admit it weighed on me quite a bit, like the other times, because I want to carry projects through from A to Z.

### What I Learned

**Advanced Game Logic:**
- `Math.random()` and `Math.floor()` for random generation
- Logic functions with boolean returns to determine winners
- State management with global variables (`playerScore`, `computerScore`)

**Complex DOM Manipulation:**
- `document.getElementById()` for specific elements
- `document.querySelector()` for CSS selectors
- `innerText` for dynamic content updates
- `style.display` for conditional show/hide

**Event Handling Mastery:**
- `addEventListener()` with callback functions
- Multiple event listeners for different buttons
- Event handling for game reset and restart

**Conditional Game Flow:**
- Ternary operators for dynamic messages
- Complex if/else logic to determine results
- Game state checks for game end

**Function Architecture:**
- `getRandomComputerResult()` - computer random selection
- `hasPlayerWonTheRound()` - victory logic
- `getRoundResults()` - complete round management
- `showResults()` - UI update
- `resetGame()` - initial state restoration

**JavaScript Patterns Observed:**
- Return statements with complex logic
- Template literals for dynamic messages
- Boolean logic for game mechanics

### Reflection

This project really validated my vademecum! It was incredible to see how every concept fit together perfectly and how the vademecum guided me through every step.

***

**Next Project**: Learn Basic String and Array Methods by Building a Music Player
