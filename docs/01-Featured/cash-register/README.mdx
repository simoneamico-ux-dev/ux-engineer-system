---
sidebar_position: 2
sidebar_label: 'Cash Register'
title: 'Cash Register'
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Cash Register

<video 
  autoPlay 
  loop 
  muted 
  playsInline 
  width="100%" 
  style={{borderRadius: '6px'}}
  src={require('./assets/cash-register.webm').default} 
/>
![Cash Register Project Mobile UI Preview](./assets/cash-register-1.webp)
![Cash Register Project Desktop UI Preview](./assets/cash-register-2.webp)

### The Project

Cash Register developed with vanilla JavaScript and OOP architecture, implementing greedy algorithms, precise integer calculations, and complete drawer state management. An application that demonstrates validation patterns, optimal change calculation, and skeuomorphic design.

### Source Code

<Tabs>
<TabItem value="html" label="index.html" default>

```html
<!-- DESIGN
   ------
   * This file contains the HTML structure of the Cash Register application.
   * The architecture follows this flow:
   * - Head with meta tags and the Google Font 'Roboto Mono', was chosen as the font to realistically simulate the
   *   monospaced, dot-matrix style of a thermal receipt printer
   * - Body containing a <main> element
   * - Inside <main>, a primary <div> ".receipt-container" is used to display the SVG background (the paper receipt)
   * - A child <div> ".receipt-content" is nested inside the container to hold all the actual UI content (text, form, etc.).

   * The UI content itself is semantically structured into three main blocks:
   * - .receipt-header: Contains the main title and terminal ID
   * - .receipt-body: Contains the core interactive elements (form, input, button) and the output area (#change-due)
   * - .receipt-footer: Contains the closing messages and transaction ID.
-->

<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Cash Register</title>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="styles.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@300;400;700&display=swap" rel="stylesheet">
    </head>

    <body>

        <!-- 
            * freeCodeCamp instructions:
            * - You should have an input element with an id of "cash"
            * - You should have a div, span, or p element with an id of "change-due"
            * - You should have a button element with an id of "purchase-btn"
        -->
 
        <main>

            <div class="receipt-container">
                <div class="receipt-content">
                    <div class="receipt-header">
                        <h1>CASH REGISTER</h1>
                        <p>TERMINAL #09-FCC</p>
                    </div>
                    <div class="receipt-body">
                        <div class="total">
                            <p>TOTAL</p>
                            <p>$19.50</p>
                        </div>
                        <p>-------------------------------</p>
                        <form id="cash-register-form">
                            <label for="cash">CASH PAID</label>
                            <div class="user-input">
                                <input type="number" id="cash" name="cash" placeholder="0.00" step="0.01" min="0" required>
                            </div>
                            <button type="button" id="purchase-btn" class="cta-button">PROCESS PAYMENT</button>
                        </form>
                        <p>-------------------------------</p>
                        <div id="change-due"></div>
                        <p>-------------------------------</p>
                    </div>
                    <div class="receipt-footer">
                        <p class="tribute"><a href="https://www.freecodecamp.org/" target="_blank" rel="noopener noreferrer" aria-label="Visit freeCodeCamp website (opens in new tab)">Trans. ID: FCC-CERT-PROJECT-09</a></p>
                        <p>THANKS FOR VISITING!</p>
                    </div>
                </div>
            </div>

        </main>

        <script src="script.js"></script>
    </body>    

</html>
```
</TabItem>

<TabItem value="css" label="styles.css">

```css
/* DESIGN
   ------
   * I chose to make the mobile version the default (Mobile-First approach),
   * with the desktop version being an override in a single media query
   * for the same reasons as the other project, namely:
   * - Mobile traffic is dominant, so it should be the primary experience
   * - Mobile devices load only the base CSS and variables, ensuring a
   *   faster load time, which is critical for mobile connections.

   * The architecture is a "Utility-First Variables" system:
   * - All values (fonts, margins, sizes) are centralized in :root variables,
   *   organized by UNIVERSAL and MOBILE (default)
   * - The DESKTOP media query only overrides these variables, which keeps
   *   the CSS ruleset minimal and highly maintainable.

   * The layout is a hybrid:
   * - A single ".receipt-container" is centered using position: absolute
   *   to hold the SVG receipt background
   * - Inside, ".receipt-content" uses display: flex with
   *   flex-direction: column to manage the internal UI flow
   * - Techniques (like radial-gradient for input dots)
   *   are used to achieve the skeuomorphic "receipt" design.
*/


/* UNIVERSAL (common variables to mobile and desktop) */

:root {
    /* Background */ 
    --background: url("https://github.com/user-attachments/assets/6b1734ff-6f1c-4c9f-8b5d-463daea23f85"); /* incredibly lightweight background, weighs only 70 KB */
    --font-color: #000;
    
    /* Font */
    --font-family: "Roboto Mono", monospace, sans-serif;
    --font-weight-light: 300;
    --font-weight-normal: 400;
    --font-weight-medium: 500;

    /* Color */
    --input-dots-hover-button-color: #6b6b6a;
    
    /* Animation */
    --result-animation: slideInFade 0.4s cubic-bezier(0.2, 1, 0.3, 1);
}


/* MOBILE (default version) */

:root {
    /* Receipt frame */
    --receipt-image: url("https://github.com/user-attachments/assets/c16ae536-5d58-41d2-8712-d52244549792"); /* the receipt is also incredibly lightweight, only 2 KB, I created it in Figma */
    --receipt-width: 377.702px;
    --receipt-height: 662.999px;
    --receipt-position-top: 50%;
    --receipt-position-left: 50%;
    --aspect-ratio: 377.702 / 662.999;

    /* Layout & Position */
    --receipt-padding: 0 54px 24px 44px;
    --title-margin-top: 110px;
    --subtitle-margin-top: 5px;
    --total-1-margin-top: 85px;
    --hyphens-1-margin-top: 0px;
    --label-margin-top: 28px;
    --input-margin-top: 4px;
    --button-margin-top: 12px;
    --hyphens-2-margin-top: 24px;
    --result-margin-top: 3px;
    --hyphens-3-margin-top: 5px;
    --footer-block-margin-top: 16px; 
    --tribute-margin-top: 2px;
    --footer-margin-top: 12px;

    /* Font Size */
    --font-size-base: 18px;
    --font-size-title: 24px;
    --font-size-small: 16px;
    --font-size-result: 14px;
    --font-size-hyphens: 15px;
    --font-size-x-small: 12px;

    /* Input */
    --input-padding: 5px 0;
    --input-dots-width: 28%;
    --input-dots-height: 2px;
    --input-dots-size: 1.2px;
    --input-dots-spacing: 8px;

    /* Button */
    --button-padding: 2px 0;
    --button-underline-thickness: 1.5px;
    --button-underline-offset: 8px;

    /* Result */
    --result-min-height: 2em;
}


/* Total CSS reset to avoid unexpected behavior across different browsers */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background-image: var(--background);
    background-size: cover;
    background-repeat: no-repeat;
    min-height: 100vh; /* Fallback per browser che non supportano dvh */
    min-height: 100dvh; /* L'unità del viewport dinamico per iOS */
    background-attachment: fixed;
    background-position: 65% 0%;
    font-family: var(--font-family);
    color: var(--font-color);
    font-weight: var(--font-weight-light);
    font-size: var(--font-size-base);
    overflow: hidden;
}

.receipt-container {
    background-image: var(--receipt-image);
    position: absolute;
    top: var(--receipt-position-top);
    left: var(--receipt-position-left);
    transform: translate(-50%, -50%);
    width: var(--receipt-width);
    background-repeat: no-repeat;
    background-size: 100% 100%;
    max-height: var(--receipt-height);
    aspect-ratio: var(--aspect-ratio);
}

.receipt-content {
    display: flex;
    flex-direction: column;
    text-align: center;
    padding: var(--receipt-padding);
}

.receipt-header {
    margin-top: var(--title-margin-top);
    transform: rotate(0.674deg);
}

.receipt-footer {
    width: 100%;
    margin-top: var(--footer-block-margin-top);
}

h1 {
    font-size: var(--font-size-title);
    font-weight: var(--font-weight-normal);
}

button {
    font-weight: var(--font-weight-medium);
}

.receipt-header > p, .receipt-footer > p:nth-child(2) {
    font-size: var(--font-size-small);
}

.receipt-header > p {
    margin-top: var(--subtitle-margin-top);
}

.total {
  display: flex;
  justify-content: space-between;
  width: 100%;
  margin-top: var(--total-1-margin-top);
}

.total > p {
    margin-top: 0; 
    font-weight: var(--font-weight-medium);
}
.total > p:nth-child(2) {
    margin-top: 0;
}

.receipt-body > p {
    font-size: var(--font-size-hyphens);
}

.receipt-body > p:nth-child(2) {
    margin-top: var(--hyphens-1-margin-top);
    transform: rotate(0.561deg);
}
.receipt-body > p:nth-child(4) {
    margin-top: var(--hyphens-2-margin-top);
    transform: rotate(0.371deg);
}
.receipt-body > p:nth-child(6) {
    margin-top: var(--hyphens-3-margin-top);
}


#cash-register-form {
    margin-top: var(--label-margin-top);
    text-align: left;
    font-weight: var(--font-weight-normal);
}

.user-input {
    position: relative;
}

.user-input::before {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: var(--input-dots-width);
    height: var(--input-dots-height);
    background-image: radial-gradient(
        circle,
        var(--input-dots-hover-button-color) var(--input-dots-size),
        transparent 1px
    );
    background-size: var(--input-dots-spacing) 100%;
    background-repeat: repeat-x;
    background-position: bottom left;
}

input {
    margin-top: var(--input-margin-top);
    border: none;
    background: transparent;
    width: 100%;
    padding: var(--input-padding);
    font-family: var(--font-family);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-medium);
}

input:focus {
    outline: none;
}

/* these next two rules hide the typical input field arrows (0.00 ▲ ▼)
 * on all WebKit-based browsers (like Safari, Chrome, Edge) */
input::-webkit-outer-spin-button,
input::-webkit-inner-spin-button {
  -webkit-appearance: none;
  margin: 0;
}

input[type=number] { /* does the same thing as the block above, but for Firefox */
  -moz-appearance: textfield;
}

button {
    margin-top: var(--button-margin-top);
    background: transparent;
    border: none;
    cursor: pointer;
    font-family: var(--font-family);
    font-size: var(--font-size-base);
    text-decoration: underline;
    text-decoration-thickness: var(--button-underline-thickness);
    text-underline-offset: var(--button-underline-offset);
    padding: var(--button-padding);
    color: black;
}

#change-due {
    margin-top: var(--result-margin-top);
    min-height: var(--result-min-height);
    font-size: var(--font-size-result);
    font-weight: var(--font-weight-medium);
}

.tribute {
    margin-top: var(--tribute-margin-top);
    font-size: var(--font-size-x-small);
}
.tribute a {
    color: var(--font-color);
    text-decoration: none;
}

.receipt-footer > p:nth-child(2) {
    margin-top: var(--footer-margin-top);
}


button:hover {
    color: var(--input-dots-hover-button-color);
}


/* DESKTOP (override of mobile variables and addition of receipt-frame) */

@media (min-width: 600px) and (min-height: 850px) {

:root {
    /* Receipt frame */
    --receipt-image: url("https://github.com/user-attachments/assets/272d1484-ae7b-4027-ae06-ce93290a36b5");
    --receipt-width: 447.315px;
    --receipt-height: 785.195px;
    --receipt-position-top: 55%;
    --receipt-position-left: 70%;
    --aspect-ratio: 377.702 / 662.999;

    /* Layout & Position */
    --receipt-padding: 0 64px 24px 54px;
    --title-margin-top: 130px;
    --subtitle-margin-top: 10px;
    --total-1-margin-top: 112px;
    --label-margin-top: 32px;
    --button-margin-top: 20px;
    --hyphens-2-margin-top: 38px;
    --result-margin-top: 12px;
    --hyphens-3-margin-top: 12px;
    --tribute-margin-top: 8px;
    --footer-margin-top: 16px;

    /* Font Size */
    --font-size-hyphens: 17.7px;

    /* Result */
    --result-min-height: 2em;
}
}
```
</TabItem>

<TabItem value="js" label="script.js">

```js
/* DESIGN
   ------
  * This file contains the validation logic for Cash Register
  * The main architectural decisions include:
  *
  * - Class-based Architecture (OOP): The choice to use a `class CashRegister`
  *   encapsulates all the operational logic. This approach promotes separation of
  *   responsibilities, so while the `constructor` handles the initial preparation of the
  *   drawer, the subsequent methods (`processPurchase`, `#calculateChange`) manage
  *   the change calculation phases. This promotes a more organized and modular code.
  *   I admit I used them in the first place because I wanted to practice, as it's a concept
  *   that excited me from the first moment I encountered it, much like what happened with variables
  *   in the CSS context. However, I first tried to understand if it was a good approach for this
  *   specific case, and luckily, it turned out to be.
  *
  * - Calculations with integers: JavaScript doesn't count the way we do, for example, 0.1 + 0.2
  *   for it is 0.30000000000000004 instead of 0.3). This is because while we humans count with ten
  *   fingers (base-10), computers count using billions of ON/OFF switches (base-2); it's like trying
  *   to weigh exactly 0.1 grams on a scale having only 1g, 2g, 4g, etc., weights available. 
  *   You can't be perfectly precise, and this imprecision accumulates in calculations.
  *   To get around this very problem, all monetary operations were performed using cents (integers).
  *   The conversion from dollars to cents happens at the input, and the final re-conversion for the
  *   output is in dollars. This approach guarantees maximum calculation precision.
  *
  * - Data structure (Map): I'm referring to the management of "cashInDrawer".
  *   I used this approach because Map would give me more consistent code.
  *   I'll add an example below to quickly show what I mean:
  *   If I had used objects:
  *   let pennies = drawer.PENNY;
  *   let hundreds = drawer["ONE HUNDRED"];
  *   Using Map:
  *   let pennies = this.cashInDrawer.get("PENNY");
  *   let hundreds = this.cashInDrawer.get("ONE HUNDRED");
  *
  * - Greedy algorithm for change: 
  *   For the change calculation process, I used a greedy algorithm.
  *   This means that, starting from the highest value denomination (thanks to the
  *   pre-sorted "DENOMINATIONS"), the system tries to dispense the largest possible
  *   number of that bill/coin, and then progressively moves to the lower value
  *   ones, until the change due is depleted.
  *   It's a strategy that makes the "best choice at the moment," and for standard monetary
  *   systems, it always guarantees the optimal result. This isn't always true, in fact,
  *   imagining an atypical monetary system with denominations of 1, 7, and 10 cents, to give
  *   15 in change, the greedy approach would end up giving 6 coins (1x10 + 5x1), while the
  *   optimal solution would have been 3 coins (1x7 + 1x7 + 1x1).
  *   I had used the same approach in the Roman Numeral Converter.
*/


/* freeCodeCamp instructions:
 * - You should have a global variable called price.
 * - You should have a global variable called cid.
 * - When #cash < price, alert "Customer does not have enough money to purchase the item".
 * - When #cash == price, #change-due shows "No change due - customer paid with exact cash".
 * - Test (price 19.5, cash 20, standard cid): #change-due shows "Status: OPEN QUARTER: $0.5".
 * - Test (price 3.26, cash 100, standard cid): #change-due shows "Status: OPEN TWENTY: $60 TEN: 
 *   $20 FIVE: $15 ONE: $1 QUARTER: $0.5 DIME: $0.2 PENNY: $0.04".
 * - Test (price 19.5, cash 20, cid[PENNY, 0.01]): #change-due shows "Status: INSUFFICIENT_FUNDS".
 * - Test (price 19.5, cash 20, cid[PENNY, 0.01, ONE, 1]): #change-due shows "Status: INSUFFICIENT_FUNDS".
 * - Test (price 19.5, cash 20, cid[PENNY, 0.5]): #change-due shows "Status: CLOSED PENNY: $0.5".
 */


const cashInput = document.getElementById("cash");
const purchaseBtn = document.getElementById("purchase-btn");
const result = document.getElementById("change-due");

let price = 19.5;
let cid = [["PENNY", 1.01], ["NICKEL", 2.05], ["DIME", 3.1], ["QUARTER", 4.25], ["ONE", 90], ["FIVE", 55], ["TEN", 20], ["TWENTY", 60], ["ONE HUNDRED", 100]];
const DENOMINATIONS = [["ONE HUNDRED", 10000], ["TWENTY", 2000], ["TEN", 1000], ["FIVE", 500], ["ONE", 100], ["QUARTER", 25], ["DIME", 10], ["NICKEL", 5], ["PENNY", 1]];

class CashRegister {

    constructor(cid) {
        this.cashInDrawer = new Map();
        this.totalInDrawer = 0; // Total cash in drawer, in cents

        cid.forEach(([currencyName, amountInDollars]) => { // first we destructure
            const amountInCents = Math.round(amountInDollars * 100); // round because it rounds to the nearest integer, e.g., 4.4 -> 4, 4.5 -> 5
            this.cashInDrawer.set(currencyName, amountInCents); // sets the cash in drawer exactly like in cid but this time the values are in cents
            this.totalInDrawer += amountInCents; // since we started from zero (this.totalInDrawer = 0) we now sum and assign with the values
        });

        this.sortedCashInDrawer = new Map(); // we create a new map because we want the values to be sorted like in DENOMINATIONS, so from largest to smallest (greedy algorithm)
        DENOMINATIONS.forEach(([name, value]) => {
          if(this.cashInDrawer.has(name)) { // we check if it actually exists in the this.cashInDrawer map
            this.sortedCashInDrawer.set(name, this.cashInDrawer.get(name)); // we can't use += because it's only used when working with numbers, .set() is its equivalent when working with maps. So with .get() we read the value from the original map (cashInDrawer), while with set we write that value to the new map (sortedCashInDrawer)
          }
        })
    }

    processPurchase(priceInCents, cashGivenInCents) {
      let changeDueInCents = cashGivenInCents - priceInCents;

      const originalChangeDue = changeDueInCents;

      if (this.totalInDrawer < changeDueInCents) {
        return {status: "INSUFFICIENT_FUNDS", change: []};
      }

      else {
        let changeToGive = []; // here we save the change to give
        this.sortedCashInDrawer.forEach((amountInDrawer, name) => { // we iterate over the denominations in the drawer, from largest to smallest (thanks to sortedCashInDrawer)
          let amountToReturn = 0;
          const currencyValue = DENOMINATIONS.find(denom => denom[0] === name) [1]; // finds the value in cents of the current denomination (e.g., 2000 for "TWENTY")
          let availableAmount = amountInDrawer; // copy of the available amount of money in the drawer

          while (changeDueInCents >= currencyValue && availableAmount > 0) { // With this loop, it keeps taking this denomination as long as the change is sufficient and as long as there is some in the drawer
            changeDueInCents -= currencyValue; // subtract from the total change due
            availableAmount -= currencyValue; // subtract from the stock of this denomination
            amountToReturn += currencyValue; // add to the partial total to return
          }

          if (amountToReturn > 0) {
            changeToGive.push([name, amountToReturn / 100]) // we convert cents to dollars for the output
          }
        });
        
        if (changeDueInCents > 0) { // here we check if (after the loop) there is remaining change, which means we didn't have the right coins/bills
          return {status: "INSUFFICIENT_FUNDS", change: []};
        }

        if (this.totalInDrawer === originalChangeDue) { // finally we check if the change due emptied the drawer, how? by comparing with the original total
          return {status: "CLOSED", change: changeToGive};
        }

        else {
          return {status: "OPEN", change: changeToGive};
        }

      }
    }

}


cashInput.addEventListener("blur", () => { // I noticed that on my iPhone 12 mini, so on iOS, after clicking the input and then exiting, the receipt stays scrolled up, this way we bring it back to the original state
  window.scrollTo(0, 0);
});

cashInput.addEventListener("keydown", (event) => {
  if (event.key === "Enter") {
    event.preventDefault();
    purchaseBtn.click();
  }
});


purchaseBtn.addEventListener("click", () => { 
  const register = new CashRegister(cid); // We create a new class instance every time the user clicks the button, this is essential because the freeCodeCamp tests will modify the global "cid" variable to run the test
  const cashGiven = parseFloat(cashInput.value); // we read the value from the input (which is a string, e.g., "20") and convert it to a number (e.g., 20.0) using parseFloat() to be able to do calculations

  // we convert everything to cents for "safe" calculations
  const priceInCents = Math.round(price * 100);
  const cashGivenInCents = Math.round(cashGiven * 100); 

  if (isNaN(cashGiven) || cashGiven <= 0 ) {
    alert("Please enter a valid positive number");
    cashInput.value = "";
    return;
  } 
  
  else if (cashGivenInCents < priceInCents) {
    alert("Customer does not have enough money to purchase the item");
    cashInput.value = "";
    return;
  }
  
  else if (cashGivenInCents === priceInCents) {
    result.textContent = "No change due - customer paid with exact cash";
    cashInput.value = "";
    return;
  }
  
  else { // this happens when the customer paid more, so we calculate the change
    const transactionResult = register.processPurchase(priceInCents, cashGivenInCents); // This is the line that "activates" the main logic of our cash register. We call the 'processPurchase' method (the "brain" that does all the calculations) passing it the price and the cash received. The method will return a "report" (an object) that contains the 'status' of the transaction and, if necessary, the 'change' (the calculated change)
    
    if (transactionResult.status === "INSUFFICIENT_FUNDS") { // we format the output based on the status
      result.textContent = "Status: INSUFFICIENT_FUNDS";
    
    } else if (transactionResult.status === "OPEN" || transactionResult.status === "CLOSED") {
      let changeText = transactionResult.change.map(item => `${item[0]}: $${item[1]}`).join(" ");
      result.textContent = `Status: ${transactionResult.status} ${changeText}`;
    }
    cashInput.value = "";
  }
});
```

</TabItem> 

<TabItem value="commented" label="explained">

```html
<!DOCTYPE html>
<!-- 🎯 HTML5 DECLARATION: "Welcome to the cash register simulator!" -->
<!-- It's like announcing the opening of a digital store where money transactions happen! 💵 -->

<html lang="en">
<!-- 🌍 MAIN CONTAINER: Our virtual receipt and cash register -->
<!-- lang="en" = English, the language of this transaction interface! -->

    <head>
        <!-- 🧠 THE COMMAND CENTER: Where we prepare our receipt-style application -->
        <!-- This is like setting up the cash register before opening the store! ⚙️ -->
        
        <title>Cash Register</title>
        <!-- 📑 TAB TITLE: Simple and direct like a receipt header -->
        <!-- What customers will see in their browser tab -->
        
        <meta charset="UTF-8" />
        <!-- 📝 CHARACTER ENCODING: UTF-8 supports currency symbols worldwide! -->
        <!-- Ensures we can display $ and other currency characters properly -->
        
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <!-- 📱 RESPONSIVE DESIGN: "This register works on any device!" -->
        <!-- Ensures the receipt properly scales on all screens from phones to desktops -->
        
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <!-- 🚀 PRECONNECTIONS: Speed optimization for font loading -->
        <!-- Like preparing the ink before printing the receipt - improves performance -->
        
        <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@300;400;700&display=swap" rel="stylesheet">
        <!-- 🔤 FONT LOADING: Roboto Mono with multiple weights -->
        <!-- This monospace font mimics the look of traditional receipt printers -->
        <!-- 300, 400, 700 weights provide light, normal and bold options -->
        
        <style>
          /* 🎨 ===== CSS STYLES: The Receipt Design ===== */
          
          /* 📱 ===== RESPONSIVE STRATEGY ===== */
          /* 🔄 MOBILE-FIRST APPROACH: Default styling is for mobile devices */
          /* Just like modern stores that prioritize mobile payments! */
          /* Mobile loads only essential CSS - faster on slower connections */
          
          
          /* 🌐 ===== UNIVERSAL VARIABLES: Shared Across All Devices ===== */
          
          :root {
              /* 🖼️ BACKGROUND */ 
              --background: url("https://github.com/user-attachments/assets/6b1734ff-6f1c-4c9f-8b5d-463daea23f85"); 
              /* 🖼️ LIGHT BACKGROUND: Lightweight image at only 70KB */
              /* Creates a subtle textured background for our receipt */
              
              --font-color: #000;
              /* ⚫ BLACK TEXT: Like traditional thermal receipt printers */
              
              /* 🔤 TYPOGRAPHY */
              --font-family: "Roboto Mono", monospace, sans-serif;
              /* 🖨️ MONOSPACED FONT: Mimics dot-matrix thermal printers */
              /* Every character takes the same width, like real receipts */
              
              --font-weight-light: 300;
              /* 🔤 LIGHT WEIGHT: For subtle, secondary text */
              
              --font-weight-normal: 400;
              /* 🔤 NORMAL WEIGHT: Standard receipt text */
              
              --font-weight-medium: 500;
              /* 🔤 MEDIUM WEIGHT: For emphasis on important details */
          
              /* 🎨 COLOR ACCENTS */
              --input-dots-hover-button-color: #6b6b6a;
              /* 🔘 DARK GRAY: For interactive elements when hovered */
              /* Subtle color change provides feedback without being flashy */
              
              /* ✨ ANIMATIONS */
              --result-animation: slideInFade 0.4s cubic-bezier(0.2, 1, 0.3, 1);
              /* 🎞️ SMOOTH ENTRANCE: For displaying change calculation results */
              /* Creates a pleasant transition when results appear */
          }
          
          
          /* 📱 ===== MOBILE VERSION: Default Settings ===== */
          
          :root {
              /* 📃 RECEIPT DESIGN */
              --receipt-image: url("https://github.com/user-attachments/assets/c16ae536-5d58-41d2-8712-d52244549792"); 
              /* 📃 RECEIPT BACKGROUND: Ultra lightweight at only 2KB */
              /* Custom-created in Figma to look like thermal paper */
              
              --receipt-width: 377.702px;
              /* 📏 RECEIPT WIDTH: Sized like a standard receipt */
              
              --receipt-height: 662.999px;
              /* 📏 RECEIPT HEIGHT: Tall enough for all transaction details */
              
              --receipt-position-top: 50%;
              --receipt-position-left: 50%;
              /* 📍 POSITIONING: Centered in the viewport */
              
              --aspect-ratio: 377.702 / 662.999;
              /* 📐 PROPORTIONS: Maintains shape when resizing */
          
              /* 📏 LAYOUT & POSITIONING */
              --receipt-padding: 0 54px 24px 44px;
              /* 📏 INNER SPACING: Comfortable margins inside the receipt */
              /* Left/right asymmetry mimics real-world printing imperfections */
              
              --title-margin-top: 110px;
              /* ⬆️ TITLE POSITION: Space at the top of the receipt */
              
              --subtitle-margin-top: 5px;
              /* ⬆️ SUBTITLE SPACE: Small gap after the main title */
              
              --total-1-margin-top: 85px;
              /* ⬆️ TOTAL POSITION: Significant space before the amount */
              
              --hyphens-1-margin-top: 0px;
              /* ⬆️ FIRST DIVIDER: No additional space needed */
              
              --label-margin-top: 28px;
              /* ⬆️ LABEL SPACE: Generous margin before input sections */
              
              --input-margin-top: 4px;
              /* ⬆️ INPUT POSITION: Small gap after its label */
              
              --button-margin-top: 12px;
              /* ⬆️ BUTTON SPACE: Comfortable margin after the input field */
              
              --hyphens-2-margin-top: 24px;
              /* ⬆️ SECOND DIVIDER: Separates input from results */
              
              --result-margin-top: 3px;
              /* ⬆️ RESULT POSITION: Minimal space after the divider */
              
              --hyphens-3-margin-top: 5px;
              /* ⬆️ THIRD DIVIDER: After displaying results */
              
              --footer-block-margin-top: 16px; 
              /* ⬆️ FOOTER AREA: Space before the closing section */
              
              --tribute-margin-top: 2px;
              /* ⬆️ ATTRIBUTION SPACE: Minimal gap for credit line */
              
              --footer-margin-top: 12px;
              /* ⬆️ CLOSING MESSAGE: Space before final thank you */
          
              /* 🔤 TYPOGRAPHY SIZES */
              --font-size-base: 18px;
              /* 📝 BASE FONT: Standard size for most content */
              
              --font-size-title: 24px;
              /* 📝 TITLE SIZE: Larger for the header */
              
              --font-size-small: 16px;
              /* 📝 SMALL TEXT: For secondary information */
              
              --font-size-result: 14px;
              /* 📝 RESULT TEXT: Slightly smaller for detailed change information */
              
              --font-size-hyphens: 15px;
              /* 📝 DIVIDER SIZE: For the dashed line separators */
              
              --font-size-x-small: 12px;
              /* 📝 SMALLEST TEXT: For attribution and fine print */
          
              /* 📝 INPUT STYLING */
              --input-padding: 5px 0;
              /* 📏 INPUT SPACING: Comfortable area for typing */
              
              --input-dots-width: 28%;
              /* 📏 DOTTED LINE: Width of the input underline */
              
              --input-dots-height: 2px;
              /* 📏 DOT HEIGHT: Thickness of the dotted line */
              
              --input-dots-size: 1.2px;
              /* 📏 DOT SIZE: Diameter of each dot in the underline */
              
              --input-dots-spacing: 8px;
              /* 📏 DOT SPACING: Gap between dots */
          
              /* 🔘 BUTTON STYLING */
              --button-padding: 2px 0;
              /* 📏 BUTTON SPACING: Minimal padding for clean look */
              
              --button-underline-thickness: 1.5px;
              /* 📏 UNDERLINE SIZE: Thickness of the button's underline */
              
              --button-underline-offset: 8px;
              /* 📏 UNDERLINE POSITION: Space between text and its underline */
          
              /* 📋 RESULT DISPLAY */
              --result-min-height: 2em;
              /* 📏 RESULT AREA: Minimum height for the change display */
              /* Ensures space is reserved even before results appear */
          }
          
          
          
          /* 🧹 ===== CSS RESET: Clean Slate ===== */
          
          * {
              margin: 0;
              padding: 0;
              box-sizing: border-box;
              /* 📦 UNIVERSAL RESET: Ensures consistent rendering across browsers */
              /* Like zeroing the cash register at the start of the day */
          }
          
          body {
              background-image: var(--background);
              /* 🖼️ BACKGROUND IMAGE: Sets the textured backdrop */
              
              background-size: cover;
              /* 📏 FULL COVERAGE: Background extends across the entire viewport */
              
              background-repeat: no-repeat;
              /* 🔄 NO REPEATING: Image appears only once */
              
              min-height: 100vh; /* Fallback for browsers that don't support dvh */
              min-height: 100dvh; /* Dynamic viewport height for mobile browsers */
              /* 📏 FULL HEIGHT: Ensures the background fills the entire screen */
              /* dvh handles mobile browser address bar changes */
              
              background-attachment: fixed;
              /* 📌 FIXED POSITION: Background stays put during scrolling */
              
              background-position: 65% 0%;
              /* 📍 POSITIONING: Slightly offset for visual interest */
              
              font-family: var(--font-family);
              /* 🔤 TYPOGRAPHY: Monospace font mimics receipt printers */
              
              color: var(--font-color);
              /* ⚫ TEXT COLOR: Black text like thermal printing */
              
              font-weight: var(--font-weight-light);
              /* 🔤 FONT WEIGHT: Light weight for most text */
              
              font-size: var(--font-size-base);
              /* 🔤 FONT SIZE: Base size for standard content */
              
              overflow: hidden;
              /* 🚫 PREVENT OVERFLOW: Keeps the interface clean */
          }
          
          .receipt-container {
              background-image: var(--receipt-image);
              /* 🧾 RECEIPT BACKGROUND: The thermal paper image */
              
              position: absolute;
              /* 📍 ABSOLUTE POSITIONING: For precise placement */
              
              top: var(--receipt-position-top);
              left: var(--receipt-position-left);
              /* 📍 CENTERING: Positioned at viewport center */
              
              transform: translate(-50%, -50%);
              /* 🔄 CENTERING TRICK: Shifts by half its dimensions for perfect centering */
              
              width: var(--receipt-width);
              /* 📏 RECEIPT WIDTH: Set by the variable */
              
              background-repeat: no-repeat;
              /* 🔄 SINGLE IMAGE: Background appears only once */
              
              background-size: 100% 100%;
              /* 📏 FULL STRETCH: Background covers the entire container */
              
              max-height: var(--receipt-height);
              /* 📏 HEIGHT LIMIT: Maximum height constraint */
              
              aspect-ratio: var(--aspect-ratio);
              /* 📐 PROPORTIONS: Maintains the receipt's shape when resizing */
          }
          
          .receipt-content {
              display: flex;
              /* 📦 FLEXIBLE LAYOUT: Modern approach to content arrangement */
              
              flex-direction: column;
              /* 📦 STACKED CONTENT: Items arranged vertically */
              
              text-align: center;
              /* ⚖️ CENTERED TEXT: Most receipt content is centered */
              
              padding: var(--receipt-padding);
              /* 📏 INNER SPACING: Comfortable margins within the receipt */
          }
          
          .receipt-header {
              margin-top: var(--title-margin-top);
              /* ⬆️ TOP SPACING: Pushes header down from the top edge */
              
              transform: rotate(0.674deg);
              /* 🔄 SLIGHT ROTATION: Creates an imperfect, realistic look */
              /* Real receipts often print slightly askew */
          }
          
          .receipt-footer {
              width: 100%;
              /* 📏 FULL WIDTH: Footer spans the entire receipt */
              
              margin-top: var(--footer-block-margin-top);
              /* ⬆️ TOP SPACING: Gap before the footer */
          }
          
          h1 {
              font-size: var(--font-size-title);
              /* 🔤 TITLE SIZE: Larger than standard text */
              
              font-weight: var(--font-weight-normal);
              /* 🔤 NORMAL WEIGHT: Not too bold, like real receipts */
          }
          
          button {
              font-weight: var(--font-weight-medium);
              /* 🔤 MEDIUM WEIGHT: Slightly bolder for emphasis */
          }
          
          .receipt-header > p, .receipt-footer > p:nth-child(2) {
              font-size: var(--font-size-small);
              /* 🔤 SMALLER TEXT: For secondary information */
              /* Applies to both terminal ID and thank you message */
          }
          
          .receipt-header > p {
              margin-top: var(--subtitle-margin-top);
              /* ⬆️ TOP SPACING: Small gap after the title */
          }
          
          .total {
            display: flex;
            /* 📦 FLEXIBLE LAYOUT: Allows for easy alignment */
            
            justify-content: space-between;
            /* ⚖️ SPACED APART: "TOTAL" on left, amount on right */
            
            width: 100%;
            /* 📏 FULL WIDTH: Spans the entire receipt */
            
            margin-top: var(--total-1-margin-top);
            /* ⬆️ TOP SPACING: Significant gap above the total */
          }
          
          .total > p {
              margin-top: 0; 
              /* ⬆️ NO EXTRA SPACE: Overrides default paragraph margin */
              
              font-weight: var(--font-weight-medium);
              /* 🔤 MEDIUM WEIGHT: Emphasizes the total amount */
          }
          .total > p:nth-child(2) {
              margin-top: 0;
              /* ⬆️ NO EXTRA SPACE: Maintains alignment with left text */
          }
          
          .receipt-body > p {
              font-size: var(--font-size-hyphens);
              /* 🔤 DIVIDER SIZE: For the dashed lines */
          }
          
          .receipt-body > p:nth-child(2) {
              margin-top: var(--hyphens-1-margin-top);
              /* ⬆️ SPACE BEFORE: First divider spacing */
              
              transform: rotate(0.561deg);
              /* 🔄 SLIGHT ROTATION: First divider tilts slightly */
              /* Creates realistic imperfection like real receipts */
          }
          .receipt-body > p:nth-child(4) {
              margin-top: var(--hyphens-2-margin-top);
              /* ⬆️ SPACE BEFORE: Second divider spacing */
              
              transform: rotate(0.371deg);
              /* 🔄 SLIGHT ROTATION: Second divider tilts slightly differently */
              /* Each divider has a unique imperfection */
          }
          .receipt-body > p:nth-child(6) {
              margin-top: var(--hyphens-3-margin-top);
              /* ⬆️ SPACE BEFORE: Third divider spacing */
          }
          
          
          #cash-register-form {
              margin-top: var(--label-margin-top);
              /* ⬆️ TOP SPACING: Gap before the form */
              
              text-align: left;
              /* ⬅️ LEFT ALIGNMENT: Form fields align to the left */
              
              font-weight: var(--font-weight-normal);
              /* 🔤 NORMAL WEIGHT: Standard text for form elements */
          }
          
          .user-input {
              position: relative;
              /* 📍 RELATIVE POSITIONING: Creates context for the dotted underline */
          }
          
          .user-input::before {
              content: "";
              /* 🔍 EMPTY CONTENT: Creates a pseudo-element */
              
              position: absolute;
              /* 📍 ABSOLUTE POSITIONING: Precisely places the dotted line */
              
              bottom: 0;
              left: 0;
              /* 📍 PLACEMENT: At the bottom of the input field */
              
              width: var(--input-dots-width);
              /* 📏 DOT LINE WIDTH: Partial width creates a fill-in-the-blank look */
              
              height: var(--input-dots-height);
              /* 📏 LINE HEIGHT: Thickness of the dotted line */
              
              background-image: radial-gradient(
                  circle,
                  var(--input-dots-hover-button-color) var(--input-dots-size),
                  transparent 1px
              );
              /* 🔵 DOTTED LINE EFFECT: Creates individual dots with radial gradient */
              /* Cleverly simulates a receipt's dotted line without using border */
              
              background-size: var(--input-dots-spacing) 100%;
              /* 📏 DOT SPACING: Controls gap between dots */
              
              background-repeat: repeat-x;
              /* 🔄 HORIZONTAL REPEAT: Dots repeat only horizontally */
              
              background-position: bottom left;
              /* 📍 ALIGNMENT: Dots appear at bottom left */
          }
          
          input {
              margin-top: var(--input-margin-top);
              /* ⬆️ TOP SPACING: Small gap after the label */
              
              border: none;
              /* 🚫 NO BORDER: Clean look without default border */
              
              background: transparent;
              /* 🔍 TRANSPARENT BACKGROUND: Input blends with receipt */
              
              width: 100%;
              /* 📏 FULL WIDTH: Input spans entire form width */
              
              padding: var(--input-padding);
              /* 📏 INNER SPACING: Comfortable area for text input */
              
              font-family: var(--font-family);
              /* 🔤 MATCHING FONT: Same monospace as the rest of the receipt */
              
              font-size: var(--font-size-base);
              /* 🔤 STANDARD SIZE: Consistent with other text */
              
              font-weight: var(--font-weight-medium);
              /* 🔤 MEDIUM WEIGHT: Makes the input stand out slightly */
          }
          
          input:focus {
              outline: none;
              /* 🚫 NO OUTLINE: Removes the browser's default focus outline */
              /* Maintains the receipt aesthetic */
          }
          
          /* 🔢 ===== HIDE NUMBER INPUT CONTROLS ===== */
          
          /* these next two rules hide the typical input field arrows (0.00 ▲ ▼)
           * on all WebKit-based browsers (like Safari, Chrome, Edge)
          */
          input::-webkit-outer-spin-button,
          input::-webkit-inner-spin-button {
            -webkit-appearance: none;
            /* 🚫 HIDE ARROWS: Removes the default up/down controls */
            
            margin: 0;
            /* 🚫 NO MARGIN: Ensures no space is reserved for arrows */
          }
          
          input[type=number] { /* does the same thing as the block above, but for Firefox */
            -moz-appearance: textfield;
            /* 🚫 FIREFOX FIX: Removes number spinners in Firefox */
            /* Makes input look like a normal text field */
          }
          
          button {
              margin-top: var(--button-margin-top);
              /* ⬆️ TOP SPACING: Gap before the button */
              
              background: transparent;
              /* 🔍 TRANSPARENT BACKGROUND: Button blends with receipt */
              
              border: none;
              /* 🚫 NO BORDER: Clean look without default border */
              
              cursor: pointer;
              /* 👆 HAND CURSOR: Shows the element is clickable */
              
              font-family: var(--font-family);
              /* 🔤 MATCHING FONT: Same monospace as the rest of the receipt */
              
              font-size: var(--font-size-base);
              /* 🔤 STANDARD SIZE: Consistent with other text */
              
              text-decoration: underline;
              /* ✏️ UNDERLINED: Visual indicator that it's a button */
              
              text-decoration-thickness: var(--button-underline-thickness);
              /* 📏 UNDERLINE THICKNESS: Controls the weight of the line */
              
              text-underline-offset: var(--button-underline-offset);
              /* 📏 UNDERLINE SPACING: Gap between text and its underline */
              
              padding: var(--button-padding);
              /* 📏 INNER SPACING: Minimal padding for clean look */
              
              color: black;
              /* ⚫ TEXT COLOR: Standard black like the rest of the receipt */
          }
          
          #change-due {
              margin-top: var(--result-margin-top);
              /* ⬆️ TOP SPACING: Small gap before the results */
              
              min-height: var(--result-min-height);
              /* 📏 MINIMUM HEIGHT: Reserves space even when empty */
              
              font-size: var(--font-size-result);
              /* 🔤 RESULT SIZE: Slightly smaller for detailed information */
              
              font-weight: var(--font-weight-medium);
              /* 🔤 MEDIUM WEIGHT: Makes results stand out */
          }
          
          .tribute {
              margin-top: var(--tribute-margin-top);
              /* ⬆️ TOP SPACING: Small gap before the attribution */
              
              font-size: var(--font-size-x-small);
              /* 🔤 SMALLEST SIZE: Attribution text is subtle */
          }
          .tribute a {
              color: var(--font-color);
              /* ⚫ LINK COLOR: Same as regular text for subtlety */
              
              text-decoration: none;
              /* 🚫 NO UNDERLINE: Clean look for the attribution link */
          }
          
          .receipt-footer > p:nth-child(2) {
              margin-top: var(--footer-margin-top);
              /* ⬆️ TOP SPACING: Gap before the thank you message */
          }
          
          
          button:hover {
              color: var(--input-dots-hover-button-color);
              /* 🔘 HOVER COLOR: Subtle color change on hover */
              /* Provides visual feedback without breaking the receipt aesthetic */
          }
          
          
          /* 🖥️ ===== DESKTOP VERSION: Enhanced Receipt ===== */
          
          @media (min-width: 600px) and (min-height: 850px) {
          /* 📐 SIZE THRESHOLD: Desktop styles activate on larger screens */
          
          :root {
              /* 📃 RECEIPT DESIGN */
              --receipt-image: url("https://github.com/user-attachments/assets/272d1484-ae7b-4027-ae06-ce93290a36b5");
              /* 📃 DESKTOP RECEIPT: Different image for larger screens */
              
              --receipt-width: 447.315px;
              /* 📏 INCREASED WIDTH: Larger receipt for desktop */
              
              --receipt-height: 785.195px;
              /* 📏 INCREASED HEIGHT: Taller receipt for desktop */
              
              --receipt-position-top: 55%;
              /* ⬆️ VERTICAL POSITION: Slightly lower than center */
              
              --receipt-position-left: 70%;
              /* ⬅️ HORIZONTAL POSITION: Off-center to the right */
              /* Creates an asymmetrical layout for visual interest */
              
              --aspect-ratio: 377.702 / 662.999;
              /* 📐 PROPORTIONS: Maintains shape when resizing */
          
              /* 📏 LAYOUT & POSITION: Adjusted for larger screens */
              --receipt-padding: 0 64px 24px 54px;
              /* 📏 INCREASED PADDING: More room around the content */
              
              --title-margin-top: 130px;
              /* ⬆️ MORE TOP SPACE: Title sits lower on desktop */
              
              --subtitle-margin-top: 10px;
              /* ⬆️ MORE SUBTITLE SPACE: Increased gap between title elements */
              
              --total-1-margin-top: 112px;
              /* ⬆️ MORE TOTAL SPACE: Larger gap before the total line */
              
              --label-margin-top: 32px;
              /* ⬆️ MORE LABEL SPACE: Increased spacing before labels */
              
              --button-margin-top: 20px;
              /* ⬆️ MORE BUTTON SPACE: Larger gap before buttons */
              
              --hyphens-2-margin-top: 38px;
              /* ⬆️ MORE DIVIDER SPACE: Increased spacing for dividers */
              
              --result-margin-top: 12px;
              /* ⬆️ MORE RESULT SPACE: Larger gap before results */
              
              --hyphens-3-margin-top: 12px;
              /* ⬆️ MORE DIVIDER SPACE: Increased spacing after results */
              
              --tribute-margin-top: 8px;
              /* ⬆️ MORE TRIBUTE SPACE: Larger gap before attribution */
              
              --footer-margin-top: 16px;
              /* ⬆️ MORE FOOTER SPACE: Increased spacing in footer */
          
              /* 🔤 TYPOGRAPHY ADJUSTMENTS */
              --font-size-hyphens: 17.7px;
              /* 🔤 LARGER DIVIDERS: Increased size for dividing lines */
          
              /* 📋 RESULT DISPLAY */
              --result-min-height: 2em;
              /* 📏 RESULT AREA: Same minimum height for consistency */
          }
          }
        </style>
    </head>

    <body>
        <!-- 💵 ===== THE CASH REGISTER INTERFACE! ===== -->
        
        <main>
            <!-- 📋 MAIN CONTAINER: The entire application -->
            <!-- Like the counter area of a store where transactions happen -->
            
            <div class="receipt-container">
                <!-- 🧾 RECEIPT BACKGROUND: The thermal paper simulation -->
                <!-- This creates the visual frame for our cash register -->
                
                <div class="receipt-content">
                    <!-- 📝 RECEIPT CONTENT: All the text and interactive elements -->
                    <!-- Everything that would be printed on a real receipt -->
                    
                    <div class="receipt-header">
                        <!-- 📑 RECEIPT TOP: Store name and terminal info -->
                        
                        <h1>CASH REGISTER</h1>
                        <!-- 🏪 STORE NAME: Bold title at the top -->
                        
                        <p>TERMINAL #09-FCC</p>
                        <!-- 🔢 TERMINAL ID: Identifies the point of sale -->
                        <!-- Like the cash register number in a real store -->
                    </div>
                    
                    <div class="receipt-body">
                        <!-- 📝 RECEIPT MAIN AREA: Transaction details and inputs -->
                        
                        <div class="total">
                            <!-- 💰 PURCHASE AMOUNT: The cost before payment -->
                            
                            <p>TOTAL</p>
                            <!-- 📊 TOTAL LABEL: Left-aligned text -->
                            
                            <p>$19.50</p>
                            <!-- 💵 AMOUNT DUE: Right-aligned price -->
                            <!-- The fixed cost that needs to be paid -->
                        </div>
                        
                        <p>-------------------------------</p>
                        <!-- 📏 DIVIDER LINE: Separates sections visually -->
                        <!-- Made of hyphens like old-school receipt printers -->
                        
                        <form id="cash-register-form">
                            <!-- 📋 INPUT FORM: Collects payment information -->
                            
                            <label for="cash">CASH PAID</label>
                            <!-- 🏷️ INPUT LABEL: Describes what to enter -->
                            
                            <div class="user-input">
                                <!-- 📝 INPUT CONTAINER: Holds the cash input field -->
                                
                                <input type="number" id="cash" name="cash" placeholder="0.00" step="0.01" min="0" required>
                                <!-- 💵 CASH INPUT: Where user enters payment amount -->
                                <!-- type="number" = numeric input only -->
                                <!-- step="0.01" = allows cents (two decimal places) -->
                                <!-- min="0" = prevents negative values -->
                                <!-- required = field cannot be empty -->
                            </div>
                            
                            <button type="button" id="purchase-btn" class="cta-button">PROCESS PAYMENT</button>
                            <!-- 🔘 ACTION BUTTON: Triggers the payment processing -->
                            <!-- type="button" prevents form submission -->
                            <!-- Clicking this calculates the change due -->
                        </form>
                        
                        <p>-------------------------------</p>
                        <!-- 📏 DIVIDER LINE: Separates input from results -->
                        
                        <div id="change-due"></div>
                        <!-- 📋 RESULTS AREA: Where change calculation appears -->
                        <!-- Initially empty, filled by JavaScript -->
                        
                        <p>-------------------------------</p>
                        <!-- 📏 DIVIDER LINE: Separates results from footer -->
                    </div>
                    
                    <div class="receipt-footer">
                        <!-- 📑 RECEIPT BOTTOM: Credits and closing message -->
                        
                        <p class="tribute"><a href="https://www.freecodecamp.org/" target="_blank" rel="noopener noreferrer" aria-label="Visit freeCodeCamp website (opens in new tab)">Trans. ID: FCC-CERT-PROJECT-09</a></p>
                        <!-- 🔢 TRANSACTION ID: Unique reference number -->
                        <!-- Links to freeCodeCamp as attribution -->
                        <!-- target="_blank" opens in a new tab -->
                        <!-- rel="noopener noreferrer" prevents security issues -->
                        
                        <p>THANKS FOR VISITING!</p>
                        <!-- 👋 CLOSING MESSAGE: Friendly goodbye -->
                        <!-- Like the "Thank you for your purchase" on real receipts -->
                    </div>
                </div>
            </div>
        </main>

        <script>
          /* 💵 ===== JAVASCRIPT: The Cash Register Logic! ===== */
          
          /* 📋 ===== APPLICATION ARCHITECTURE ===== */
          /* This uses object-oriented programming (OOP) with a class-based approach */
          /* Just like a real cash register has a clear system for handling money */
          
          /* 🔢 ===== NUMBER HANDLING ===== */
          /* All monetary calculations use cents (integers) instead of dollars (decimals) */
          /* This avoids JavaScript's floating-point precision problems */
          /* Like counting pennies instead of dollars for perfect accuracy! */
          
          /* 💰 ===== CHANGE CALCULATION ===== */
          /* Uses a greedy algorithm - starts with largest denominations first */
          /* Just like a cashier who hands out $20 bills before $1 bills */
          
          
          /* 🔗 ===== DOM REFERENCES: Connecting to the HTML ===== */
          
          const cashInput = document.getElementById("cash");
          /* 💵 CASH INPUT: Reference to the payment field */
          
          const purchaseBtn = document.getElementById("purchase-btn");
          /* 🔘 PURCHASE BUTTON: Reference to the action button */
          
          const result = document.getElementById("change-due");
          /* 📋 RESULT DISPLAY: Where change information appears */
          
          
          /* 💰 ===== GLOBAL VARIABLES: Main Transaction Data ===== */
          
          let price = 19.5;
          /* 💵 ITEM PRICE: Cost of the purchase ($19.50) */
          
          let cid = [["PENNY", 1.01], ["NICKEL", 2.05], ["DIME", 3.1], ["QUARTER", 4.25], ["ONE", 90], ["FIVE", 55], ["TEN", 20], ["TWENTY", 60], ["ONE HUNDRED", 100]];
          /* 💰 CASH IN DRAWER: Available currency in the register */
          /* Format: [["DENOMINATION", AMOUNT], ...] */
          /* Like the initial cash count at the start of a shift */
          
          const DENOMINATIONS = [["ONE HUNDRED", 10000], ["TWENTY", 2000], ["TEN", 1000], ["FIVE", 500], ["ONE", 100], ["QUARTER", 25], ["DIME", 10], ["NICKEL", 5], ["PENNY", 1]];
          /* 💵 CURRENCY VALUES: Denominations in cents, sorted largest to smallest */
          /* This determines the order of change distribution */
          /* The values are in cents (100 × dollar value) for precision */
          
          
          /* 🏧 ===== CASH REGISTER CLASS: The Main Logic ===== */
          
          class CashRegister {
              /* 🏧 CLASS DEFINITION: Blueprint for the register object */
              /* Like the instruction manual for operating the cash drawer */
          
              constructor(cid) {
                  /* 🔧 INITIALIZATION: Sets up the cash drawer */
                  /* Like opening the register at the start of the day */
                  
                  this.cashInDrawer = new Map();
                  /* 🗃️ MONEY STORAGE: Uses Map for consistent key access */
                  /* Think of this as the organized compartments in a cash drawer */
                  
                  this.totalInDrawer = 0; // Total cash in drawer, in cents
                  /* 💰 RUNNING TOTAL: Tracks the total money available */
                  /* We'll add to this as we count each denomination */
          
                  cid.forEach(([currencyName, amountInDollars]) => { // first we destructure
                      /* 🔄 ITERATE THROUGH CURRENCIES: Process each denomination */
                      /* Destructuring pulls out the name and amount for easier use */
                      
                      const amountInCents = Math.round(amountInDollars * 100); // round because it rounds to the nearest integer, e.g., 4.4 -> 4, 4.5 -> 5
                      /* 🔢 CONVERT TO CENTS: Multiply by 100 and round */
                      /* This prevents floating-point errors in JavaScript */
                      
                      this.cashInDrawer.set(currencyName, amountInCents); // sets the cash in drawer exactly like in cid but this time the values are in cents
                      /* 💰 STORE IN MAP: Save amount for each denomination */
                      
                      this.totalInDrawer += amountInCents; // since we started from zero (this.totalInDrawer = 0) we now sum and assign with the values
                      /* 💰 ADD TO TOTAL: Keep a running sum of all cash */
                  });
          
                  this.sortedCashInDrawer = new Map(); // we create a new map because we want the values to be sorted like in DENOMINATIONS, so from largest to smallest (greedy algorithm)
                  /* 🗂️ SORTED STORAGE: Organizes money from largest to smallest */
                  /* This is crucial for the greedy algorithm to work properly */
                  
                  DENOMINATIONS.forEach(([name, value]) => {
                    /* 🔄 ITERATE DENOMINATIONS: Process in descending order */
                    
                    if(this.cashInDrawer.has(name)) { // we check if it actually exists in the this.cashInDrawer map
                      /* 🔍 VERIFY EXISTENCE: Only add denominations we actually have */
                      
                      this.sortedCashInDrawer.set(name, this.cashInDrawer.get(name)); // we can't use += because it's only used when working with numbers, .set() is its equivalent when working with maps. So with .get() we read the value from the original map (cashInDrawer), while with set we write that value to the new map (sortedCashInDrawer)
                      /* 💰 STORE IN ORDER: Add to the sorted collection */
                      /* This creates our working copy in the right order */
                    }
                  })
              }
          
              processPurchase(priceInCents, cashGivenInCents) {
                  /* 🧮 PAYMENT PROCESSING: Calculate change based on payment */
                  /* This is the main function that handles the transaction */
                  
                let changeDueInCents = cashGivenInCents - priceInCents;
                /* 🧮 CALCULATE CHANGE: Subtract price from payment */
                /* In cents to avoid floating-point issues */
          
                const originalChangeDue = changeDueInCents;
                /* 📋 SAVE ORIGINAL: Keep the initial value for later comparison */
                /* We'll need this to determine if drawer is emptied */
          
                if (this.totalInDrawer < changeDueInCents) {
                  /* 🚫 INSUFFICIENT FUNDS CHECK: Not enough money to make change */
                  
                  return {status: "INSUFFICIENT_FUNDS", change: []};
                  /* 🚫 ERROR RESULT: Cannot complete transaction */
                  /* Returns an object with status and empty change array */
                }
          
                else {
                  /* ✅ SUFFICIENT FUNDS: Continue with change calculation */
                  
                  let changeToGive = []; // here we save the change to give
                  /* 📋 CHANGE COLLECTION: Will hold the bills/coins to return */
                  
                  this.sortedCashInDrawer.forEach((amountInDrawer, name) => { // we iterate over the denominations in the drawer, from largest to smallest (thanks to sortedCashInDrawer)
                    /* 🔄 ITERATE DENOMINATIONS: Process from largest to smallest */
                    /* This is the greedy algorithm in action */
                    
                    let amountToReturn = 0;
                    /* 💰 RUNNING COUNT: Tracks amount of this denomination to give */
                    
                    const currencyValue = DENOMINATIONS.find(denom => denom[0] === name) [1]; // finds the value in cents of the current denomination (e.g., 2000 for "TWENTY")
                    /* 🔢 GET DENOMINATION VALUE: Find the cent value of this currency */
                    /* For example: "TWENTY" is worth 2000 cents */
                    
                    let availableAmount = amountInDrawer; // copy of the available amount of money in the drawer
                    /* 💰 AVAILABLE CASH: How much of this denomination we have */
          
                    while (changeDueInCents >= currencyValue && availableAmount > 0) { // With this loop, it keeps taking this denomination as long as the change is sufficient and as long as there is some in the drawer
                      /* 🔄 TAKE BILLS/COINS WHILE POSSIBLE: */
                      /* Continue as long as denomination fits into change needed */
                      /* AND we still have this denomination available */
                      
                      changeDueInCents -= currencyValue; // subtract from the total change due
                      /* 🧮 REDUCE CHANGE NEEDED: Denomination has been allocated */
                      
                      availableAmount -= currencyValue; // subtract from the stock of this denomination
                      /* 🧮 REDUCE AVAILABLE: We've used one of these bills/coins */
                      
                      amountToReturn += currencyValue; // add to the partial total to return
                      /* 🧮 INCREASE RETURN: Count this in our change to give */
                    }
          
                    if (amountToReturn > 0) {
                      /* 💰 CHECK IF USED: Did we use any of this denomination? */
                      
                      changeToGive.push([name, amountToReturn / 100]) // we convert cents to dollars for the output
                      /* 📋 ADD TO CHANGE LIST: Include this in the change to return */
                      /* Converts back to dollars for human-readable output */
                    }
                  });
                  
                  if (changeDueInCents > 0) { // here we check if (after the loop) there is remaining change, which means we didn't have the right coins/bills
                    /* 🚫 CHANGE IMPOSSIBLE: Can't make exact change with available denominations */
                    
                    return {status: "INSUFFICIENT_FUNDS", change: []};
                    /* 🚫 ERROR RESULT: Cannot complete transaction */
                  }
          
                  if (this.totalInDrawer === originalChangeDue) { // finally we check if the change due emptied the drawer, how? by comparing with the original total
                    /* 🔄 DRAWER EMPTIED: Change equals all money in drawer */
                    
                    return {status: "CLOSED", change: changeToGive};
                    /* 🔒 CLOSED RESULT: Drawer is now empty */
                    /* This is a special case in the challenge */
                  }
          
                  else {
                    /* ✅ NORMAL CHANGE: Drawer still has money left */
                    
                    return {status: "OPEN", change: changeToGive};
                    /* ✅ SUCCESS RESULT: Transaction complete, drawer still operational */
                  }
          
                }
              }
          
          }
          
          
          /* 📱 ===== MOBILE FIXES: Platform-Specific Adjustments ===== */
          
          cashInput.addEventListener("blur", () => { // I noticed that on my iPhone 12 mini, so on iOS, after clicking the input and then exiting, the receipt stays scrolled up, this way we bring it back to the original state
            /* 📱 HANDLE INPUT BLUR: When user finishes with the input field */
            /* Specific fix for iOS scroll position issues */
            
            window.scrollTo(0, 0);
            /* 📜 RESET SCROLL: Move viewport back to the top */
            /* Prevents receipt from staying scrolled on mobile */
          });
          
          /* ⌨️ ===== KEYBOARD SUPPORT: Enhanced User Experience ===== */
          
          cashInput.addEventListener("keydown", (event) => {
            /* ⌨️ KEYBOARD DETECTION: Listen for key presses in the input */
            
            if (event.key === "Enter") {
              /* ⏎ ENTER KEY: User pressed Enter to submit */
              
              event.preventDefault();
              /* 🛑 PREVENT DEFAULT: Stop form submission behavior */
              
              purchaseBtn.click();
              /* 🔘 TRIGGER BUTTON: Simulate clicking the process button */
              /* Allows for keyboard-only operation */
            }
          });
          
          
          /* 🔘 ===== BUTTON CLICK HANDLER: Process the Transaction ===== */
          
          purchaseBtn.addEventListener("click", () => { 
            /* 🖱️ BUTTON CLICK: User wants to process the payment */
            
            const register = new CashRegister(cid); // We create a new class instance every time the user clicks the button, this is essential because the freeCodeCamp tests will modify the global "cid" variable to run the test
            /* 🏧 CREATE REGISTER: New instance for this transaction */
            /* Fresh instance ensures accurate state for each test case */
            
            const cashGiven = parseFloat(cashInput.value); // we read the value from the input (which is a string, e.g., "20") and convert it to a number (e.g., 20.0) using parseFloat() to be able to do calculations
            /* 💵 GET PAYMENT: Convert input string to number */
            
            // we convert everything to cents for "safe" calculations
            const priceInCents = Math.round(price * 100);
            const cashGivenInCents = Math.round(cashGiven * 100); 
            /* 🔢 CONVERT TO CENTS: Avoid floating-point calculation errors */
            /* Multiplication and rounding ensures exact cent values */
          
            if (isNaN(cashGiven) || cashGiven <= 0 ) {
              /* 🚫 INVALID INPUT CHECKS: */
              /* Not a number OR negative/zero value */
              
              alert("Please enter a valid positive number");
              /* ⚠️ ERROR MESSAGE: Inform user of the problem */
              
              cashInput.value = "";
              /* 🧹 CLEAR INPUT: Reset for a new attempt */
              
              return;
              /* 🛑 STOP EXECUTION: Don't continue processing */
            } 
            
            else if (cashGivenInCents < priceInCents) {
              /* 🚫 INSUFFICIENT PAYMENT: Customer didn't pay enough */
              
              alert("Customer does not have enough money to purchase the item");
              /* ⚠️ ERROR MESSAGE: Inform user of the problem */
              
              cashInput.value = "";
              /* 🧹 CLEAR INPUT: Reset for a new attempt */
              
              return;
              /* 🛑 STOP EXECUTION: Don't continue processing */
            }
            
            else if (cashGivenInCents === priceInCents) {
              /* ⚖️ EXACT PAYMENT: No change needed */
              
              result.textContent = "No change due - customer paid with exact cash";
              /* 📝 SUCCESS MESSAGE: Inform that no change is needed */
              
              cashInput.value = "";
              /* 🧹 CLEAR INPUT: Reset for a new attempt */
              
              return;
              /* 🛑 STOP EXECUTION: No need for change calculation */
            }
            
            else { // this happens when the customer paid more, so we calculate the change
              /* 💰 CHANGE NEEDED: Calculate and display change */
              
              const transactionResult = register.processPurchase(priceInCents, cashGivenInCents); // This is the line that "activates" the main logic of our cash register. We call the 'processPurchase' method (the "brain" that does all the calculations) passing it the price and the cash received. The method will return a "report" (an object) that contains the 'status' of the transaction and, if necessary, the 'change' (the calculated change)
              /* 🧮 PROCESS TRANSACTION: Call the main calculation method */
              /* This determines if change is possible and calculates denominations */
              
              if (transactionResult.status === "INSUFFICIENT_FUNDS") { // we format the output based on the status
                /* 🚫 NOT ENOUGH FUNDS: Can't make exact change */
                
                result.textContent = "Status: INSUFFICIENT_FUNDS";
                /* 📝 ERROR MESSAGE: Inform that transaction failed */
              
              } else if (transactionResult.status === "OPEN" || transactionResult.status === "CLOSED") {
                /* ✅ TRANSACTION OK: Either normal change or drawer emptied */
                
                let changeText = transactionResult.change.map(item => `${item[0]}: $${item[1]}`).join(" ");
                /* 📝 FORMAT CHANGE: Create readable list of denominations */
                /* Converts [["QUARTER", 0.5]] to "QUARTER: $0.5" */
                
                result.textContent = `Status: ${transactionResult.status} ${changeText}`;
                /* 📝 SUCCESS MESSAGE: Show status and change breakdown */
              }
              cashInput.value = "";
              /* 🧹 CLEAR INPUT: Reset for a new transaction */
            }
          });
        </script>
    </body>    
</html>

<!-- 🎯 ===== CASH REGISTER SUMMARY ===== -->
<!-- 
💵 APP ARCHITECTURE:
│
├── 🧠 HEAD (setup)
│   └── 🎨 CSS with thermal receipt design:
│       ├── 📱 Mobile-first responsive approach
│       ├── 📄 Custom receipt background images
│       ├── 📏 Precise positioning with CSS variables
│       └── ✨ Visual details like dotted lines and slight rotation
│
├── 📄 BODY (interface)
│   └── 🧾 Receipt layout
│       ├── 📑 Receipt header (store name, terminal)
│       ├── 💰 Transaction details (price, payment input)
│       ├── 🔘 Process payment button
│       └── 📋 Change calculation results area
│
└── 🧮 JAVASCRIPT (logic)
    ├── 🏧 CashRegister class with methods:
    │   ├── constructor (sets up cash drawer)
    │   └── processPurchase (calculates change)
    ├── 💵 Transaction status handling:
    │   ├── OPEN: Normal change given
    │   ├── CLOSED: Drawer emptied exactly
    │   └── INSUFFICIENT_FUNDS: Can't make change
    └── 🧪 Input validation and event handling

⚙️ KEY TECHNIQUES:
├── 💰 Cents-based calculations for precision
├── 🔄 Greedy algorithm for optimal change
├── 🗃️ Map data structure for cash drawer
├── 📱 Mobile-specific UX fixes
└── 🎨 Skeuomorphic receipt design

It's like having a virtual cash register that makes perfect change every time! 💵✨
-->
```
</TabItem>

</Tabs>

### The Achievement: Google UX Certificate

I earned the Google UX Certificate by completing the last 2 courses I was missing and creating the other 2 required projects to obtain it.<br />
These were two very intense weeks and I'm really happy with what I accomplished.

### The Cash Register Project

In certification projects like this one, I document most of the technical choices directly in the code, including discoveries made during development. There's not much to add here then.<br />
Nevertheless, I want to highlight some aspects. First of all, I'm thrilled to have created incredibly lightweight images without sacrificing graphics.<br />

**The SVG Receipt - 2 KB:**<br />
The result was achieved thanks to Figma, where I created the receipt with a rectangle and the pen tool. Being native SVGs, I simply grouped the various elements and exported as SVG. Result: **2 KB**!

**The Optimized Background - 70 KB:**<br />
Initially I had chosen a different image: a table with a slice of bread and lettuce leaf. Unfortunately it didn't offer good contrast and weighed 2 MB. So I conducted a new search in stock photo libraries, taking the opportunity to update my Tools folder on the desktop with the best image tools.<br />
I took inspiration from an illustration and used Imagen (tool in Gemini workspace). Since the initial quality was poor, I applied upscaling with one of the tools in the folder. Then, thanks to ImageOptim, the image further decreased in weight while maintaining intact aesthetics.<br />
Result: **highest quality with a weight of only 70 KB**!

**The Logic with OOP Classes:**<br />
I documented everything inside the code. I wanted to use classes to practice and fortunately it proved to be an excellent choice, although it's a more complex solution than necessary compared to what's required to pass the freeCodeCamp tests.

### The Other Two Projects for Google UX

**Maintenance App Website:**<br />
The first was a simple website. I decided not to follow other tracks, but rather to revisit the Maintenance App imagining I was selling that service. Here's the result:

<details>
  <summary><strong>Maintenance App Website, mobile and desktop version</strong></summary>
  <strong>Desktop Version</strong>
  <br />
  <br />
  ![Maintenance App Desktop Website](./assets/cash-register-4.webp)
  <br />
  <br />
  <br />
  <strong>Mobile Version</strong>
  <br />
  <br />
  <img 
    src={require('./assets/cash-register-5.webp').default} 
    alt="Maintenance App Mobile Website" 
    style={{width: '60%'}}
  />
</details>


### Mosaic: The Heart Project

But now let's get to the project where I left my heart: **Mosaic**.<br />
It was easy to understand the needs having two psychologists in the family.
You can find the Figma prototype below, so you can explore the entire experience:
<table>
<tr>
<td width="50%">
  <img 
    src={require('./assets/cash-register-3.webp').default} 
    alt="Mosaic Project Preview (Home Screen)" 
    style={{width: '100%'}}
  />
</td>
<td width="50%">
  <h3>Interactive Prototype</h3>
  <p>Explore all screens and flows of the app in the complete Figma prototype.</p>
  <br />
  <a href="https://www.figma.com/proto/HDYGy1YlsayxweWqN7sBJE/Mosaic?node-id=1-27&p=f&t=wfrcP529dntLd1bU-1&scaling=scale-down&content-scaling=fixed&page-id=0%3A1&starting-point-node-id=1%3A27">
    <b>→ Open Figma Prototype</b>
  </a>
</td>
</tr>
</table>
<br />
I really want to emphasize that this is not a tool designed to replace the therapist's active listening process. The goal, on the contrary, is to integrate into the current workflow. The ideal process involves the therapist taking their usual notes, on paper or device, and then "dumping" to Mosaic as spontaneously as possible what happened during the session: the AI will take care of structuring and writing it clearly. This would ensure considerable organization and save time at home recomposing the various pieces, because Mosaic does it for you. Additionally, it helps optimize the time between sessions: instead of finding yourself with a few scattered words in a notebook and having to reconstruct the thread of events later, or even worse at home, you can immediately capture what happened and then, while preparing for the next patient, take a quick look at their overview to rapidly refresh your memory. This can become a true new way of working, more fluid and sustainable.

Mosaic does not provide any advice, not even in the Full Analysis. The choice of therapeutic direction will always and only remain in the therapist's hands. I absolutely do not support replacement tools, like tools that listen to meetings or similar and act as a "second brain". We must continue to trust our abilities, otherwise we risk them atrophying in exchange for an initial immediate gain.

**Lightweight Liquid Glass and Accessibility:**<br />
I used a very light Liquid Glass effect. As much as I'm among the people who like it aesthetically, I believe it makes important compromises with visual accessibility.<br />

Here's how Apple used the tab bar in the Music app. I want to clarify that in most iOS applications the effect is not tone-on-tone like in this example:<br />
<br />
<img 
    src={require('./assets/cash-register-6.webp').default} 
    alt="Apple Music tab bar" 
    style={{width: '40%'}}
/>
<br />
<br />
And how I decided to use it very lightly in Mosaic:<br />
<br />
<img 
    src={require('./assets/cash-register-7.webp').default} 
    alt="Mosaic tab bar" 
    style={{width: '40%'}}
/>

Starting with iOS 26.1, Apple still allows users to disable it in the settings and return to the standard blur effect. Unfortunately, as we learned in the Google course, this isn’t the best approach: complying with accessibility standards doesn’t just help a few it benefits everyone. I find it reasonable to make it optional, but even this detail already suggests that something isn’t quite right.
<br />

**The Philosophy: Reversing Expectations:**<br />
Regarding the open source side, I wanted to play with expectations and expected graphics. If you ask someone to imagine an open source app of this kind, they'll imagine un-"Apple" graphics, complexity, and consequent difficulty of use.<br />
If instead we asked to imagine an app that has a subscription business model of over 30 euros per month, integrated AI, and all these premium features, one would expect exactly Mosaic's graphics.<br />
So I opted for an iOS style, albeit reinterpreted in some aspects, with absolute simplicity as the main priority. All of this, however, completely Open Source.
It’s incredible how a project like this is already truly feasible today by using locally running models such as Microsoft Phi-4 Mini. <br />
Let me explain: <br />
I should note that, since I’m not a backend developer, I might be mistaken, but after several rounds of research I found that Phi-4 Mini would be the ideal candidate for this kind of task. It delivers excellent output quickly and the only drawback is its size over 7GB but for a therapist who cares about privacy, this isn’t a problem.
For phones with hardware weaker than an iPhone 15 Pro, lighter models such as Phi-3 Mini or Llama 3.2 3B could be used, and they weigh only around 2GB.
As for transcription, fortunately there’s OpenAI Whisper Base: it’s about 140MB, open-source, and provides excellent transcriptions!

### What I Learned

**Advanced OOP Architecture:**
- ES6 classes to encapsulate complex operational logic
- `constructor()` for initialization and data preparation
- Private methods (`#calculateChange`) to protect internal logic
- Separation of responsibilities between preparation and calculation

**Modern Data Structures:**
- `Map` instead of objects for more consistent key-value management
- Uniform data access: `.get()` and `.set()` vs mixed notations
- Iteration with `.forEach()` on Map for cleaner logic

**Greedy Algorithms:**
- "Best choice at the moment" strategy for optimal change calculation
- Pre-sorting denominations from largest to smallest
- `while` loop to dispense maximum number of each denomination
- Understanding limitations: works only with standard monetary systems

**Precise Calculations with Integers:**
- Floating-point problem: `0.1 + 0.2 !== 0.3` in JavaScript
- Dollars → cents conversion at input for precision
- `Math.round()` for safe rounding
- Cents → dollars reconversion only at output

**Robust Input Validation:**
- Guard clauses for precondition checks
- `isNaN()` to validate numbers
- Edge case handling: exact cash, insufficient, greater
- Informative alerts for immediate feedback

**Transaction State Management:**
- `INSUFFICIENT_FUNDS` status when impossible to give change
- `CLOSED` status when change completely empties the drawer
- `OPEN` status for standard transactions with partial change
- Return of structured objects: `{status, change}`

**Image Optimization:**
- Native SVGs from Figma: 2 KB for vector graphics
- Imagen (Gemini) for illustration generation
- AI upscaling to improve quality without weight
- ImageOptim for final compression

**Skeuomorphic Design:**
- Radial gradient to simulate input dots on receipt
- `background-size` and `background-repeat` for patterns
- Roboto Mono font to simulate thermal printer
- Transform rotate for realistic imperfections

**Mobile-First with CSS Variables:**
- All values centralized in `:root` variables
- Media query that overrides only necessary variables
- `100vh` + `100dvh` fallback to handle iOS dynamic URL bar
- Conditional loading: different SVG receipts for mobile/desktop

**Advanced Event Handling:**
- `blur` event for iOS scroll fix after input focus
- `keydown` with `Enter` for alternative submit
- `preventDefault()` to control form behavior
- Class instance recreated at each click for freeCodeCamp tests

**Functional Array Methods:**
- `.find()` to search denomination value in DENOMINATIONS
- `.forEach()` to iterate on Map cashInDrawer
- `.map()` to format change array into string
- `.join()` to concatenate output strings

***

**Next Project**: Learn Fetch and Promises by Building an fCC Authors Page